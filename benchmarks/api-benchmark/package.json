{
  "name": "akan-benchmarks",
  "private": true,
  "type": "module",
  "description": "Performance benchmark harness for akanjs: cross-framework comparison + internal tracing.",
  "scripts": {
    "bench": "bun harness/run.ts",
    "bench:all": "bun harness/run.ts --all --suite rest,fullstack",
    "coldstart": "bun harness/coldstart.ts",
    "report": "bun report/generate.ts",
    "regression": "bun regression/check.ts"
  },
  "dependencies": {
    "elysia": "^1.1.0",
    "fastify": "^5.0.0",
    "hono": "^4.6.0",
    "jose": "^6.2.3"
  }
}
