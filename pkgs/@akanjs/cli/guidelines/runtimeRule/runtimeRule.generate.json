{
  "title": "Runtime Rule",
  "description": "Akan build and runtime surfaces — web/csr toggles, the gateway/solo process model, logging, the generated image, and shipped assets",
  "group": "core",
  "codegenPriority": 4,
  "deprecated": false,
  "scans": [
    {
      "type": "example",
      "description": "App and lib runtime declarations",
      "path": "{apps,libs}/*/akan.config.ts",
      "sample": 4
    }
  ],
  "update": {
    "filePath": "./runtimeRule.instruction.md",
    "contents": ["web surface toggles", "process model", "logging and live tail", "generated image", "shipped assets"],
    "rules": [
      "Use current Akan APIs only",
      "State the rule and the reason it exists, never restate a type signature",
      "Name the env that narrows a build-time declaration wherever one exists",
      "Keep it workspace-neutral — no app or repo names"
    ]
  }
}
